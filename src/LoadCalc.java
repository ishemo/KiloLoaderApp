public class LoadCalc {

    

    // checks if loadable (divisible by 2.5)
   public static boolean checkIfLoadable(double weight) {
       return weight % 2.5 == 0;
   } 

   //
   public static double getLoadableWeight(double weight) {
       return 2.0;
   }

   /**public static Node[] findBestLoadScheme(double weight) {
       if(!(checkIfLoadable(weight))) {
           return 
       }

   }**/

    
}
